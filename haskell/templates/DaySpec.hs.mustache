-- |
module Main where

import AoC (testDay)
import Test.Tasty (TestTree, defaultMain, testGroup)

import Day{{day_s}} (parts)

tests :: [TestTree]
tests = [testDay {{year}} Day{{day_s}}.parts]

-- main
main :: IO ()
main = do
  defaultMain (testGroup "AoE {{year}}" tests)
